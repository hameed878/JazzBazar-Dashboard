{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "npm ci --no-audit --no-fund --prefer-offline && npm run build && rm -rf node_modules && npm ci --omit=dev --no-audit --no-fund --prefer-offline"
  },
  "deploy": {
    "startCommand": "node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
